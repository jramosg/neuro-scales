---
import { getLangFromUrl, useTranslations } from '@i18n/utils'
import { gcsIndex } from '@scales/gcs'
import FormProgressBar from '@components/FormProgressBar.astro'
import ScaleResultsCard from '@components/ScaleResultsCard.astro'
import type { LevelInfo, Zone } from '@components/ScaleResultsCard.astro'

const lang = getLangFromUrl(Astro.url)
const t = useTranslations(lang)

const gcsLevels: LevelInfo[] = [
  {
    key: 'mild',
    label: t('gcs.result.mild'),
    desc: t('gcs.result.mild.desc'),
    colorIndex: 0,
  },
  {
    key: 'moderate',
    label: t('gcs.result.moderate'),
    desc: t('gcs.result.moderate.desc'),
    colorIndex: 2,
  },
  {
    key: 'severe',
    label: t('gcs.result.severe'),
    desc: t('gcs.result.severe.desc'),
    colorIndex: 4,
  },
]

const gcsZones: Zone[] = [
  { from: 13, to: 15, colorIndex: 0, label: t('gcs.results.range.mild') },
  { from: 9, to: 12, colorIndex: 2, label: t('gcs.results.range.moderate') },
  { from: 3, to: 8, colorIndex: 4, label: t('gcs.results.range.severe') },
]
---

<div class='gcs-wrapper'>
  <form id='gcs-form' novalidate>
    <FormProgressBar
      countId='gcs-progress-count'
      fillId='gcs-progress-fill'
      label={t('gcs.progress')}
    />

    <div class='activities'>
      {
        gcsIndex.activities.map((activity, idx) => (
          <fieldset
            class='activity-group'
            style={`animation-delay: ${idx * 60}ms`}
            data-activity={activity.id}
          >
            <legend class='activity-label'>
              <span class='activity-num'>
                {String(idx + 1).padStart(2, '0')}
              </span>
              {t(activity.labelKey)}
            </legend>

            {activity.noteKey && (
              <p class='activity-note'>{t(activity.noteKey)}</p>
            )}

            <div class='options'>
              {activity.options.map((opt) => (
                <label class='option-label'>
                  <input
                    type='radio'
                    name={activity.id}
                    value={String(opt.value)}
                    class='option-radio'
                  />
                  <span class='option-card'>
                    <span class='option-score'>{opt.value}</span>
                    <span class='option-text'>{t(opt.labelKey)}</span>
                  </span>
                </label>
              ))}
            </div>
          </fieldset>
        ))
      }
    </div>

    <div class='form-actions'>
      <button
        type='submit'
        id='gcs-calculate-btn'
        class='btn-calculate'
        disabled
        aria-describedby='gcs-calculate-hint'
      >
        {t('gcs.calculate')}
      </button>
      <p id='gcs-calculate-hint' class='calculate-hint'>
        {t('gcs.calculate-hint')}
      </p>
      <p class='privacy-note'>{t('form.privacy')}</p>
    </div>
  </form>
</div>

<ScaleResultsCard
  maxScore={15}
  maxLabel={t('gcs.results.max')}
  scoreLabel={t('gcs.results.score')}
  interpretationTitle={t('results.interpretation')}
  rangesTitle={t('gcs.results.ranges')}
  retakeLabel={t('results.retake')}
  backHref={`/${lang}`}
  backLabel={t('results.back')}
  levels={gcsLevels}
  zones={gcsZones}
  gaugeMarks={[3, 8, 12, 15]}
/>

<script>
  import { scoreGCS, gcsIndex } from '../scales/gcs'
  import { setupFormProgress } from '../utils/form'

  function initGCSForm() {
    setupFormProgress({
      formId: 'gcs-form',
      calculateBtnId: 'gcs-calculate-btn',
      calculateHintId: 'gcs-calculate-hint',
      progressCountId: 'gcs-progress-count',
      progressFillId: 'gcs-progress-fill',
      totalItems: gcsIndex.activities.length,
      onCalculate: (answers) => {
        const result = scoreGCS(answers)
        const form = document.getElementById('gcs-form')
        if (form) form.hidden = true
        document.dispatchEvent(
          new CustomEvent('scale:result', {
            detail: { score: result.score, level: result.level },
          })
        )
      },
    })
  }

  initGCSForm()
  document.addEventListener('astro:after-swap', initGCSForm)
</script>

<style>
  .gcs-wrapper {
    max-width: 720px;
    margin: 0 auto;
    padding: var(--s6) 0 var(--s4);
  }

  /* ── Activities ── */
  .activities {
    display: flex;
    flex-direction: column;
    gap: var(--s4);
  }

  .activity-group {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--s5);
    background: var(--color-surface);
    animation: fadeUp 400ms ease both;
    transition: border-color var(--transition-fast);
  }

  .activity-group:focus-within {
    border-color: var(--color-teal);
  }

  .activity-label {
    display: flex;
    align-items: baseline;
    gap: var(--s3);
    font-family: var(--font-serif);
    font-size: 1.05rem;
    color: var(--color-text);
    margin-bottom: var(--s2);
    width: 100%;
  }

  .activity-num {
    font-family: var(--font-sans);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    color: var(--color-text-dim);
    flex-shrink: 0;
    padding-top: 2px;
  }

  .activity-note {
    font-size: 0.8rem;
    color: var(--color-text-dim);
    font-style: italic;
    line-height: 1.5;
    margin-bottom: var(--s4);
    padding-left: calc(var(--s3) + 28px);
    border-left: 2px solid var(--color-border-subtle);
    padding-top: 2px;
    padding-bottom: 2px;
  }

  /* ── Options ── */
  .options {
    display: flex;
    flex-direction: column;
    gap: var(--s2);
  }

  .option-label {
    display: block;
    cursor: pointer;
  }

  .option-radio {
    position: absolute;
    opacity: 0;
    width: 0;
    height: 0;
  }

  .option-card {
    display: flex;
    align-items: flex-start;
    gap: var(--s3);
    padding: var(--s3) var(--s4);
    border: 1px solid var(--color-border-subtle);
    border-radius: var(--radius-md);
    background: var(--color-surface-raised);
    transition: all var(--transition-fast);
    cursor: pointer;
  }

  .option-label:hover .option-card {
    border-color: var(--color-border);
    background: var(--color-surface-hover);
  }

  .option-card.selected {
    border-color: var(--color-accent);
    background: var(--color-accent-dim);
  }

  .option-score {
    flex-shrink: 0;
    min-width: 28px;
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--color-text-dim);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: 1px 6px;
    text-align: center;
    line-height: 1.6;
    transition: all var(--transition-fast);
  }

  .option-card.selected .option-score {
    color: var(--color-accent);
    border-color: var(--color-accent);
    background: var(--color-accent-dim);
  }

  .option-text {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    line-height: 1.5;
    transition: color var(--transition-fast);
  }

  .option-card.selected .option-text {
    color: var(--color-text);
  }

  /* ── Submit ── */
  .form-actions {
    margin-top: var(--s7);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--s3);
  }

  .btn-calculate {
    padding: var(--s3) var(--s7);
    background: var(--color-accent);
    color: var(--color-bg);
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    border: none;
    border-radius: var(--radius-md);
    cursor: pointer;
    transition: all var(--transition-fast);
  }

  .btn-calculate:hover:not(:disabled) {
    background: var(--color-accent-light);
    transform: translateY(-1px);
  }

  .btn-calculate:disabled {
    opacity: 0.35;
    cursor: not-allowed;
  }

  .calculate-hint {
    font-size: 0.8rem;
    color: var(--color-text-dim);
  }

  .privacy-note {
    font-size: 0.75rem;
    color: var(--color-text-dim);
    margin-top: var(--s2);
  }
</style>
